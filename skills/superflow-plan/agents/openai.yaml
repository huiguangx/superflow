interface:
  display_name: "Superflow Plan"
  short_description: "Build an evidence-backed implementation contract"
  default_prompt: "Use $superflow-plan to turn this request into an audited implementation contract."
policy:
  allow_implicit_invocation: false
